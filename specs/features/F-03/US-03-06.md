# US-03-06 · Temperature Warning Highlight

## User Story

**As a** operator monitoring battery health, **I want to** see an amber highlight on temperature values that exceed a safe threshold, **so that** I am immediately alerted to thermal conditions that may require attention.

## Description

The Temperature column displays each asset's current operating temperature in °C. When the value is at or above 28 °C, it renders in amber to signal an elevated condition requiring attention. Values below the threshold render in the standard text colour.

## Acceptance Criteria

- **AC-1**: The Temperature column is present and displays a value in °C for every non-fault asset.
- **AC-2**: Temperature values ≥ 28 °C are rendered in amber/orange colour.
- **AC-3**: Temperature values < 28 °C are rendered in the default text colour.
- **AC-4**: The threshold boundary (28 °C) is consistently applied — an asset with exactly 28.0 °C triggers the amber style.

## Test Cases

| ID | AC | Test Case | Type |
|----|-----|-----------|------|
| TC-03-06-01 | AC-1 | Verify the Temperature column displays a value in °C for every non-fault asset | E2E |
| TC-03-06-02 | AC-2 | Verify temperature values ≥ 28 °C are rendered in amber/orange colour | E2E |
| TC-03-06-03 | AC-3 | Verify temperature values < 28 °C are rendered in the default text colour | E2E |
| TC-03-06-04 | AC-4 | Verify an asset with exactly 28.0 °C triggers the amber style (boundary test) | E2E |
