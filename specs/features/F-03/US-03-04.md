# US-03-04 · Select an Asset to Drill Down

## User Story

**As a** operator who wants deeper insight into a specific battery, **I want to** click a table row to select that asset, **so that** the detail cards update to show data for the asset I selected.

## Description

Every row in the fleet table is interactive. Clicking a row marks it as selected (highlighted with a left-side accent border and a background tint) and triggers the detail cards (F-04 and F-05) to re-render with the selected asset's data. Only one row can be selected at a time. On initial load, BESS-01 is pre-selected. When an asset is selected via F-07's per-battery strip, F-03 auto-navigates to the correct page.

## Acceptance Criteria

- **AC-1**: Clicking any table row applies a visible "selected" highlight to that row.
- **AC-2**: Only one row is highlighted as selected at any time; clicking a new row deselects the previous one.
- **AC-3**: After clicking a row, the Current State card (F-04) updates to display the selected asset's data.
- **AC-4**: After clicking a row, the 24-Hour Replay card (F-05) regenerates with the selected asset's data.
- **AC-5**: The cursor changes to a pointer when hovering over any table row.
- **AC-6**: A row-hover state (light background tint) is visible on mouse-over before clicking.
- **AC-7**: On initial dashboard load, BESS-01 is pre-selected and its row is highlighted without requiring a click.
- **AC-8**: When an asset is selected via the F-07 per-battery strip (external trigger), F-03 automatically navigates to the page containing that asset so the highlighted row is always visible.

## Test Cases

| ID | AC | Test Case | Type |
|----|-----|-----------|------|
| TC-03-04-01 | AC-1 | Verify clicking a table row applies a visible "selected" highlight | E2E |
| TC-03-04-02 | AC-2 | Verify only one row is highlighted at a time; clicking a new row deselects the previous | E2E |
| TC-03-04-03 | AC-3 | Verify clicking a row updates the Current State card (F-04) with the selected asset's data | E2E |
| TC-03-04-04 | AC-4 | Verify clicking a row regenerates the 24-Hour Replay card (F-05) with the selected asset's data | E2E |
| TC-03-04-05 | AC-5 | Verify the cursor changes to a pointer when hovering over any table row | E2E |
| TC-03-04-06 | AC-6 | Verify a row-hover state with a light background tint is visible on mouse-over | E2E |
| TC-03-04-07 | AC-7 | Verify BESS-01 is pre-selected and highlighted on initial dashboard load | E2E |
| TC-03-04-08 | AC-8 | Verify selecting an asset via F-07 strip causes F-03 to auto-navigate to the correct page | E2E |
