# US-06-05 · Daily Energy Summary Chips

## User Story

**As a** operator reviewing a battery's daily performance, **I want to** see a summary of total energy charged, discharged, and net cycle count for the day, **so that** I can assess utilisation without adding up the raw data myself.

## Description

Below the scrub bar, three summary chips display daily energy totals derived from the 24-hour power series: total energy charged (kWh), total energy discharged (kWh), and net equivalent full cycles (×). These values are calculated from the same data that drives the chart and are generated fresh each time an asset is selected.

## Acceptance Criteria

- **AC-1**: Three summary chips are visible below the scrub bar: Charged (kWh), Discharged (kWh), Net Cycles (×).
- **AC-2**: All three chips display non-zero, non-empty values for every selectable asset.
- **AC-3**: The Charged value equals the sum of positive-power intervals × 0.25 h (15-minute resolution) in kWh.
- **AC-4**: The Discharged value equals the sum of absolute negative-power intervals × 0.25 h in kWh.
- **AC-5**: The Net Cycles value equals (Charged + Discharged) ÷ 2 ÷ asset capacity, rounded to one decimal place.
- **AC-6**: All three values update when a different asset is selected.

## Test Cases

| ID | AC | Test Case | Type |
|----|-----|-----------|------|
| TC-06-05-01 | AC-1 | Verify three summary chips (Charged, Discharged, Net Cycles) are visible below the scrub bar | E2E |
| TC-06-05-02 | AC-2 | Verify all three chips display non-zero, non-empty values for every selectable asset | E2E |
| TC-06-05-03 | AC-3 | Verify the Charged value equals the sum of positive-power intervals × 0.25 h in kWh | E2E |
| TC-06-05-04 | AC-4 | Verify the Discharged value equals the sum of absolute negative-power intervals × 0.25 h in kWh | E2E |
| TC-06-05-05 | AC-5 | Verify the Net Cycles value equals (Charged + Discharged) ÷ 2 ÷ asset capacity, rounded to one decimal | E2E |
| TC-06-05-06 | AC-6 | Verify all three values update when a different asset is selected | E2E |
