This directory contains individual feature specification files parsed from `specs/user-stories.md`.

Structure: `F-XX/US-XX-XX.md` — one file per user story.

Generated by the `story-parser` agent via `/parse-stories`.

Visual reference: `specs/mock-ui.html` — the static HTML mockup used as the design source of truth for all implementations.
