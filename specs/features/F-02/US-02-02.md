# US-02-02 · Available Energy Display

## User Story

**As a** operator planning the next dispatch decision, **I want to** see how much energy is immediately available across my fleet, **so that** I can judge whether the portfolio can respond to the AI recommendation.

## Description

The "Available Now" chip shows the aggregated usable energy currently stored across all non-fault assets, derived from each asset's SoC and capacity. It is dynamically computed from the fleet data and updates whenever the underlying values change.

## Acceptance Criteria

- **AC-1**: An "Available Now" chip is visible in the portfolio header on dashboard load.
- **AC-2**: The chip displays a numeric value with the unit MWh.
- **AC-3**: The value is arithmetically consistent with the sum of (SoC% × capacity) for all non-fault assets (within rounding to two decimal places).
- **AC-4**: The chip value is distinct from the Total Capacity chip value and is lower or equal to it.

## Test Cases

| ID | AC | Test Case | Type |
|----|-----|-----------|------|
| TC-02-02-01 | AC-1 | Verify an "Available Now" chip is visible in the portfolio header after signing in | E2E |
| TC-02-02-02 | AC-2 | Verify the chip displays a numeric value with the unit MWh | E2E |
| TC-02-02-03 | AC-3 | Verify the displayed value is arithmetically consistent with sum of (SoC% × capacity) for non-fault assets | E2E |
| TC-02-02-04 | AC-4 | Verify the "Available Now" value is lower than or equal to the "Total Capacity" value | E2E |
