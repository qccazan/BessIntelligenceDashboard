# US-02-04 · Live Sync Status Indicator

## User Story

**As a** operator relying on the dashboard for operational decisions, **I want to** see a live status indicator and last-sync timestamp in the header, **so that** I know the data I am viewing is current.

## Description

A small animated dot and a text string showing the number of online assets and the elapsed time since the last data sync are displayed in the portfolio header below the page title. The online asset count is computed as count(D-02 records where mode ≠ "fault"). The elapsed-time counter is cosmetic — it counts up from 0 on page load using a 1-second interval with no data fetch.

## Acceptance Criteria

- **AC-1**: A status dot is visible in the page subtitle area of the portfolio header.
- **AC-2**: The dot is rendered in a "live" colour (teal/green) indicating normal operation.
- **AC-3**: The subtitle text includes the number of online assets, computed as count(D-02 records where mode ≠ "fault"). With BESS-05 in fault, this renders as "11 assets online".
- **AC-4**: The subtitle text includes a cosmetic elapsed-time counter (e.g. "Last sync 12 seconds ago") that counts up from 0 on page load using a 1-second interval. No data fetch is performed.

## Test Cases

| ID | AC | Test Case | Type |
|----|-----|-----------|------|
| TC-02-04-01 | AC-1 | Verify a status dot is visible in the page subtitle area of the portfolio header | E2E |
| TC-02-04-02 | AC-2 | Verify the dot is rendered in teal or green colour | E2E |
| TC-02-04-03 | AC-3 | Verify the subtitle text renders "11 assets online" (excluding BESS-05 fault) | E2E |
| TC-02-04-04 | AC-4 | Verify the "Last sync" counter starts at 0 and increments every second with no data fetch | E2E |
